{
  "private": true,
  "name": "example",
  "version": "0.0.3",
  "description": "a gsw example",
  "main": "index.html",
  "pkg-state": {
    "predeploy": {
      "name": "gsw"
    },
    "postdeploy": {
      "name": "example"
    }
  },
  "scripts": {
    "test": ":",
    "start": "parcel index.html --open",
    "build": "parcel build index.html",
    "predeploy": "pkg-state predeploy && rm -rf .cache dist .DS_Store",
    "deploy": "codesandbox ./",
    "postdeploy": "pkg-state postdeploy"
  },
  "dependencies": {
    "gsw": "^0.0.3"
  },
  "devDependencies": {
    "@babel/core": "7.4.3",
    "codesandbox": "1.3.8",
    "parcel-bundler": "1.12.3",
    "pkg-state": "0.0.1",
    "typescript": "3.4.2"
  }
}
